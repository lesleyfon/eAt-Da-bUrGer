<h1>Burgerssss!!!!!!</h1>

<h2>Burger devoured</h2>

<ul>
  {{#each burgers}}
  {{!-- unless sleepy is true execute the partial --}}
    {{#unless devoured}}
      {{> burger/burger-block yum=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers to be Devoured!</h2>

<ul>
  {{#each burgers}}
  {{!-- if sleepy if false execute the partial --}}
  {{!-- partial is a snippet we can  use multiple times --}}
    {{#if devoured}}
      {{> burger/burger-block sleep=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Devourer a Burger here</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="bur" name="name">
  </div>

  <div class="form-group">
    <label for="slee">Sleepy?</label><br>
    <input type="radio" name="sleepy" value="1" checked> Sleepy!<br>
    <input type="radio" name="sleepy" value="0"> Awake
  </div>

  <button type="submit">Add </button>
  <button type="submit">Delete</button>
</form>
